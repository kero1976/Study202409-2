user.nameとuser.emailを設定しないと動かない

```
git config --global user.name "ユーザー名"
git config --global user.email メールアドレス
```

git config --global user.name "kero"
git config --global user.email kero1976@outlook.jp